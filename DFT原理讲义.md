# 密度泛函理论（DFT）原理讲义

## 0. 本讲定位

### 0.1 核心问题

> 如何从量子力学第一性原理（first principles）计算一个有机分子的电子结构——轨道能量、分子几何、振动频率、光谱性质——而不依赖任何实验参数？

在前面的讲座中，我们学会了：
- 粒子在势阱中的薛定谔方程（单粒子问题，可以精确求解）
- 量子谐振子（还是单粒子问题）
- 氢原子（一个电子 + 一个核，还是可以精确求解）

但真正的分子有**很多电子**：蒽有 94 个电子，C₆₀ 有 360 个电子，一个 P3HT 单体有 100+ 个电子。多电子问题不可能精确求解。

**DFT 是目前最成功的近似方法**——它用一个巧妙的映射把不可解的多体问题变成一组可解的单粒子方程，在精度和计算量之间取得了最佳平衡。

### 0.2 前置知识

- 薛定谔方程的含义（第二讲）
- 变分原理的基本思想（"试探函数的能量总 ≥ 真实基态能量"）
- 量子谐振子（第四讲，理解分子振动）
- 原子轨道的概念（s, p, d 轨道）

### 0.3 本讲路线图

```
多体薛定谔方程（精确但解不动）
    ↓  第一个近似
Born-Oppenheimer 近似（分离核运动和电子运动）
    ↓  还是解不动
多电子薛定谔方程（3N 维波函数，不可能存储）
    ↓  Hohenberg-Kohn 定理（精确理论）
密度泛函理论的基础（用 3 维密度替代 3N 维波函数）
    ↓  Kohn-Sham 方案（实际可计算）
等效单粒子方程 + 交换关联泛函近似
    ↓  选择基组展开
矩阵方程 → 计算机自洽求解
    ↓
输出：能量、轨道、几何、频率……
```

---

## 1. 多体问题：为什么需要 DFT

### 1.1 精确的多体薛定谔方程

考虑一个有 $M$ 个原子核和 $N$ 个电子的分子。完整的薛定谔方程是：

$$\hat{H}_{\text{total}}\,\Psi(\vec{r}_1,\ldots,\vec{r}_N;\vec{R}_1,\ldots,\vec{R}_M) = E_{\text{total}}\,\Psi$$

总 Hamiltonian 包括五项：

$$\hat{H}_{\text{total}} = \underbrace{\hat{T}_N}_{\text{核动能}} + \underbrace{\hat{T}_e}_{\text{电子动能}} + \underbrace{\hat{V}_{NN}}_{\text{核-核排斥}} + \underbrace{\hat{V}_{eN}}_{\text{电子-核吸引}} + \underbrace{\hat{V}_{ee}}_{\text{电子-电子排斥}}$$

写成具体形式：

$$\hat{T}_e = -\sum_{i=1}^{N} \frac{\hbar^2}{2m_e}\nabla_i^2$$

$$\hat{V}_{eN} = -\sum_{i=1}^{N}\sum_{A=1}^{M} \frac{Z_A e^2}{|\vec{r}_i - \vec{R}_A|}$$

$$\hat{V}_{ee} = \sum_{i<j}^{N} \frac{e^2}{|\vec{r}_i - \vec{r}_j|}$$

**关键困难**：$\hat{V}_{ee}$ 这一项把所有电子耦合在一起——第 $i$ 个电子的行为取决于**所有其他电子**的位置。这使得方程不能分离变量，无法得到解析解。

### 1.2 维度灾难

波函数 $\Psi(\vec{r}_1, \vec{r}_2, \ldots, \vec{r}_N)$ 是 $3N$ 维空间中的函数。

让我们算一下存储这个函数需要多少内存：

- 蒽：$N = 94$ 个电子 → $3 \times 94 = 282$ 个坐标
- 如果每个坐标只取 10 个格点
- 总格点数：$10^{282}$
- 每个点存一个双精度浮点数（8 bytes）
- 需要存储量：$8 \times 10^{282}$ bytes

宇宙中的原子总数约 $10^{80}$。即使把每个原子变成一个硬盘，也远远存不下 $10^{282}$ 个数。

> **结论**：对于超过几个电子的系统，直接求解多体薛定谔方程是**原则上不可能的**——不是计算机不够快，而是维度灾难使得问题在根本上不可行。

### 1.3 Born-Oppenheimer 近似：先分离核运动

第一个简化：利用核和电子的质量差异。

- 质子质量 / 电子质量 = 1836
- 核运动比电子运动慢约 $\sqrt{1836} \approx 43$ 倍

**近似**：从电子的角度看，原子核几乎是静止的。所以可以把核的位置 $\{\vec{R}_A\}$ 当作**固定参数**（而不是变量），只求解电子的运动。

这给我们**电子薛定谔方程**：

$$\hat{H}_e\,\Psi_e(\vec{r}_1,\ldots,\vec{r}_N) = E_e\,\Psi_e(\vec{r}_1,\ldots,\vec{r}_N)$$

其中电子 Hamiltonian：

$$\hat{H}_e = \hat{T}_e + \hat{V}_{eN} + \hat{V}_{ee}$$

核-核排斥 $\hat{V}_{NN}$ 对电子来说只是一个常数（核位置固定了），可以最后加回去。

> **Born-Oppenheimer 近似的意义**：把一个"电子+核"的大问题拆成两步：(1) 固定核位置，解电子问题；(2) 改变核位置，得到势能面，在势能面上研究核运动。

虽然维度从 $3(N+M)$ 降到了 $3N$，但 $3N$ 仍然是天文数字。我们还需要进一步的方法。

### 1.4 各种近似方法的定位

在 Born-Oppenheimer 近似之后，人们发展了多种方法来处理多电子问题：

| 方法 | 核心思想 | 精度 | 计算量标度 | 适用范围 |
|------|---------|------|-----------|---------|
| Hartree-Fock (HF) | 每个电子在平均场中运动 | 缺失关联能 | $O(K^4)$ | 小分子定性 |
| 组态相互作用 (CI) | 多个 Slater 行列式展开 | 可系统提高 | 指数增长 | <15 个电子 |
| 耦合簇 (CCSD(T)) | "化学精度的金标准" | 非常高 | $O(N^7)$ | <30 个原子 |
| **DFT** | **用密度代替波函数** | **好（取决于泛函）** | **$O(K^3)$** | **数百原子** |
| 半经验方法 | 用实验参数替代难算的积分 | 取决于参数化 | $O(N^2)$ | 数千原子 |

DFT 的独特地位：精度接近 CCSD(T)，但计算量仅与 HF 相当。这就是为什么它是有机电子学中**最常用**的量子化学方法。

---

## 2. Hohenberg-Kohn 定理：DFT 的理论基础

### 2.1 电子密度：从 $3N$ 维到 3 维

在进入 HK 定理之前，先明确"电子密度"的定义。

**$N$ 体波函数** $\Psi(\vec{r}_1, \vec{r}_2, \ldots, \vec{r}_N)$ 是 $3N$ 维函数，包含了系统的全部信息。

**电子密度** $\rho(\vec{r})$ 的定义：

$$\rho(\vec{r}) = N \int |\Psi(\vec{r}, \vec{r}_2, \vec{r}_3, \ldots, \vec{r}_N)|^2 \, d\vec{r}_2 \, d\vec{r}_3 \cdots d\vec{r}_N$$

物理含义：$\rho(\vec{r})\,d^3r$ = 在位置 $\vec{r}$ 附近的体积元 $d^3r$ 中**找到任意一个电子**的概率。

性质：
- $\rho(\vec{r}) \geq 0$（概率密度，非负）
- $\int \rho(\vec{r})\,d^3r = N$（总共 $N$ 个电子）
- $\rho(\vec{r})$ 只是普通三维空间中的函数——**无论系统有多少电子**

直觉：$\rho(\vec{r})$ 就是你在实验室中通过 X 射线衍射能**测量到**的电子分布。它是一个可观测量。

### 2.2 Hohenberg-Kohn 第一定理（唯一性定理，1964）

**定理陈述**：

> 对于一个 $N$ 电子系统在外势 $V_{\text{ext}}(\vec{r})$ 中的非简并基态，基态电子密度 $\rho_0(\vec{r})$ **唯一确定** $V_{\text{ext}}(\vec{r})$（差一个无关紧要的常数）。

**逆否表述**：两个不同的外势（差别不是常数）不可能给出相同的基态密度。

**这个定理为什么令人惊讶？**

从 $\Psi$ 到 $\rho$ 是一个巨大的信息压缩：$3N$ 维 → $3$ 维。按常理，压缩这么多信息一定会丢失很多东西。但 HK 定理告诉我们：**对于基态，没有任何信息丢失**。

逻辑链：$\rho_0 \to V_{\text{ext}} \to \hat{H} \to \Psi_0 \to$ 所有基态性质

也就是说，只要你知道了基态密度（一个三维函数），原则上就能推出系统的**一切基态性质**——能量、力、偶极矩、磁化率……全部。

### 2.3 第一定理的证明（反证法）

这个证明非常优美且简短，是量子力学中最漂亮的证明之一。

**证明目标**：证明映射 $V_{\text{ext}} \to \rho_0$ 是单射（一对一）的，即不同的外势给出不同的基态密度。

**用反证法**：假设存在两个不同的外势 $V_1$ 和 $V_2$（$V_1 - V_2 \neq$ 常数），它们恰好给出**相同的**基态密度 $\rho_0$。

**第一步：建立两个独立的系统**

两个外势对应两个 Hamiltonian：
$$\hat{H}_1 = \hat{T} + \hat{V}_{ee} + \sum_i V_1(\vec{r}_i)$$
$$\hat{H}_2 = \hat{T} + \hat{V}_{ee} + \sum_i V_2(\vec{r}_i)$$

它们有各自的基态波函数和基态能量：
$$\hat{H}_1\Psi_1 = E_1\Psi_1, \qquad \hat{H}_2\Psi_2 = E_2\Psi_2$$

注意 $\Psi_1 \neq \Psi_2$（因为 $V_1 \neq V_2$，如果它们的基态波函数相同，则 $(\hat{H}_1 - \hat{H}_2)\Psi = (E_1 - E_2)\Psi$ 意味着 $V_1 - V_2 = $ 常数，与假设矛盾）。

**第二步：对第一个系统用变分原理**

$\Psi_1$ 是 $\hat{H}_1$ 的基态，所以对任何其他归一化态（包括 $\Psi_2$）：

$$E_1 < \langle\Psi_2|\hat{H}_1|\Psi_2\rangle$$

（严格不等号，因为基态非简并且 $\Psi_2 \neq \Psi_1$。）

把 $\hat{H}_1 = \hat{H}_2 + \sum_i[V_1(\vec{r}_i) - V_2(\vec{r}_i)]$ 代入：

$$E_1 < \langle\Psi_2|\hat{H}_2|\Psi_2\rangle + \langle\Psi_2|\sum_i[V_1(\vec{r}_i) - V_2(\vec{r}_i)]|\Psi_2\rangle$$

第一项就是 $E_2$（$\Psi_2$ 是 $\hat{H}_2$ 的基态）。

第二项中，单体算符 $\sum_i V(\vec{r}_i)$ 的期望值可以用密度写出：

$$\langle\Psi_2|\sum_i V(\vec{r}_i)|\Psi_2\rangle = \int V(\vec{r})\,\rho_{\Psi_2}(\vec{r})\,d^3r$$

由假设，$\Psi_2$ 的基态密度也是 $\rho_0$。所以：

$$\boxed{E_1 < E_2 + \int [V_1(\vec{r}) - V_2(\vec{r})]\,\rho_0(\vec{r})\,d^3r} \tag{A}$$

**第三步：对第二个系统用变分原理**

完全对称地：

$$\boxed{E_2 < E_1 + \int [V_2(\vec{r}) - V_1(\vec{r})]\,\rho_0(\vec{r})\,d^3r} \tag{B}$$

**第四步：相加得到矛盾**

将不等式 (A) 和 (B) 相加：

$$E_1 + E_2 < E_2 + E_1 + \int [V_1 - V_2]\rho_0\,d^3r + \int [V_2 - V_1]\rho_0\,d^3r$$

右边第三项和第四项互相抵消：

$$E_1 + E_2 < E_1 + E_2$$

即 $0 < 0$。**矛盾！**

因此最初的假设不成立。$\blacksquare$

**结论**：不存在两个不同的外势给出相同的基态密度。即 $\rho_0$ 唯一确定 $V_{\text{ext}}$。

### 2.4 Hohenberg-Kohn 第二定理（变分定理）

**定理陈述**：

存在一个**普适能量泛函** $E[\rho]$，对于正确的外势 $V_{\text{ext}}$，在所有满足 $\rho(\vec{r}) \geq 0$ 且 $\int \rho\,d^3r = N$ 的试探密度中，真正的基态密度 $\rho_0$ 使 $E[\rho]$ 取最小值：

$$E_0 = E[\rho_0] \leq E[\tilde{\rho}] \quad \text{（对任意合法的试探密度 $\tilde{\rho}$）}$$

**意义**：这给了我们一个寻找基态的**操作方案**——在所有可能的密度中，找使能量泛函最小的那个。

**能量泛函的结构**：

$$E[\rho] = T[\rho] + V_{eN}[\rho] + V_{ee}[\rho]$$

其中：
- $V_{eN}[\rho] = \int V_{\text{ext}}(\vec{r})\,\rho(\vec{r})\,d^3r$（外势能，形式已知）
- $T[\rho]$：动能泛函（不知道精确形式）
- $V_{ee}[\rho]$：电子-电子相互作用泛函（不知道精确形式）

**问题**：HK 定理证明了 $E[\rho]$ 存在且可以做变分，但**没有给出** $T[\rho]$ 和 $V_{ee}[\rho]$ 的具体形式。这是一个存在性定理，不是构造性的。

### 2.5 HK 定理的局限性

1. **只对基态成立**：激发态不在 HK 定理的保证范围内（TD-DFT 用另外的定理处理）
2. **泛函形式未知**：定理说"存在一个泛函"，但没说它长什么样
3. **要求非简并基态**：对简并基态，需要额外处理
4. **$v$-可表示性问题**：不是所有满足 $\rho \geq 0, \int\rho = N$ 的函数都对应某个 $V_{\text{ext}}$ 的基态

这些局限性中最关键的是第 2 点——泛函形式未知。这就是 Kohn-Sham 方案要解决的问题。

---

## 3. Kohn-Sham 方案：化不可能为可能

### 3.1 困境

HK 定理告诉我们：原则上只需要 $\rho(\vec{r})$ 就够了。但有两个难以逾越的障碍：

**障碍一：动能泛函 $T[\rho]$**

动能依赖于波函数的**梯度**（导数），而不仅仅是密度本身。人们试过直接用密度表示动能（Thomas-Fermi 模型），但精度很差——它甚至预测不了化学键的形成！

**障碍二：电子关联**

电子-电子相互作用中的量子关联效应（电子不仅互相排斥，还因为 Pauli 原理而"相互回避"），很难用简单的密度泛函精确描述。

### 3.2 Kohn-Sham 的天才想法（1965）

Kohn 和 Sham 的核心洞察是：

> 引入一个**虚构的辅助系统**——一组无相互作用的电子，它们处于某个有效势 $V_{\text{eff}}(\vec{r})$ 中，并且这个辅助系统的基态密度**恰好等于真实系统的基态密度**。

为什么这是天才想法？

- 对于**无相互作用**的电子，多体问题**精确可解**——它分解为 $N$ 个独立的单粒子问题！
- 无相互作用系统的动能可以**精确计算**（不需要近似的动能泛函）
- 所有"不知道的东西"被集中到一个小的修正项（交换-关联能）中

### 3.3 能量泛函的 Kohn-Sham 分解

Kohn 和 Sham 把总能量泛函重新划分为：

$$E[\rho] = T_s[\rho] + \int V_{\text{ext}}(\vec{r})\rho(\vec{r})\,d^3r + E_H[\rho] + E_{\text{xc}}[\rho]$$

各项含义：

| 项 | 符号 | 含义 | 是否精确知道？ |
|----|------|------|---------------|
| 无相互作用动能 | $T_s[\rho]$ | 虚构系统中电子的动能 | **精确**（从 KS 轨道算） |
| 外势能 | $\int V_{\text{ext}}\rho\,d^3r$ | 电子与原子核的吸引 | **精确** |
| Hartree 能 | $E_H[\rho] = \frac{1}{2}\iint\frac{\rho(\vec{r})\rho(\vec{r}')}{|\vec{r}-\vec{r}'|}d^3r\,d^3r'$ | 电子间经典库仑排斥 | **精确** |
| 交换-关联能 | $E_{\text{xc}}[\rho]$ | **其余所有东西** | **不精确，需要近似** |

关键点：$E_{\text{xc}}[\rho]$ 是一个**定义**——它等于真实系统的能量减去前三项：

$$E_{\text{xc}}[\rho] \equiv (T[\rho] - T_s[\rho]) + (V_{ee}[\rho] - E_H[\rho])$$

即：
- 真实动能与无相互作用动能的**差**（关联动能）
- 真实电子-电子相互作用与经典库仑排斥的**差**（交换 + 关联）

这一项通常只占总能量的很小一部分（~5%），所以即使用近似泛函，误差也是可控的。

### 3.4 Kohn-Sham 方程的推导

现在做变分：令 $\delta E[\rho] / \delta \rho = \mu$（化学势，Lagrange 乘子来自约束 $\int\rho = N$）。

由于我们引入了无相互作用辅助系统，密度可以用**单粒子轨道**（Kohn-Sham 轨道）精确表示：

$$\rho(\vec{r}) = \sum_{i=1}^{N} |\phi_i(\vec{r})|^2$$

对能量泛函关于 $\phi_i^*$ 做变分（加上轨道正交归一的约束），得到 **Kohn-Sham 方程**：

$$\boxed{\left[-\frac{\hbar^2}{2m}\nabla^2 + V_{\text{eff}}(\vec{r})\right]\phi_i(\vec{r}) = \varepsilon_i\,\phi_i(\vec{r})}$$

其中有效势：

$$\boxed{V_{\text{eff}}(\vec{r}) = V_{\text{ext}}(\vec{r}) + V_H(\vec{r}) + V_{\text{xc}}(\vec{r})}$$

各项：

$$V_{\text{ext}}(\vec{r}) = -\sum_{A=1}^{M} \frac{Z_A e^2}{|\vec{r} - \vec{R}_A|} \qquad \text{（原子核吸引）}$$

$$V_H(\vec{r}) = e^2\int \frac{\rho(\vec{r}')}{|\vec{r} - \vec{r}'|}\,d^3r' \qquad \text{（Hartree 势 = 电子云的平均排斥）}$$

$$V_{\text{xc}}(\vec{r}) = \frac{\delta E_{\text{xc}}[\rho]}{\delta \rho(\vec{r})} \qquad \text{（交换-关联势 = $E_{\text{xc}}$ 的泛函微分）}$$

### 3.5 KS 方程的形式与意义

让我们仔细看看 KS 方程的形式：

$$\left[-\frac{\hbar^2}{2m}\nabla^2 + V_{\text{eff}}(\vec{r})\right]\phi_i(\vec{r}) = \varepsilon_i\,\phi_i(\vec{r})$$

这看起来**和氢原子的薛定谔方程一模一样**！

- 左边第一项：动能算符
- 左边第二项：某个势能
- 右边：能量本征值

但有两个关键区别：

1. **$V_{\text{eff}}$ 依赖于 $\rho$，而 $\rho$ 依赖于 $\{\phi_i\}$**——方程是**非线性**的，必须自洽求解
2. **$\phi_i$ 不是真实的电子波函数**——它们是辅助系统的轨道。严格来说，$\varepsilon_i$ 也不是真实的电子能量（但在实践中我们经常把它们当作分子轨道能量来用）

### 3.6 自洽场（SCF）迭代

因为方程是非线性的，不能一步求解，必须**迭代**：

```
1. 猜测初始密度 ρ⁰(r)（通常用原子密度叠加）

2. 从 ρ⁰ 构造 V_eff:
   V_eff = V_ext + V_H[ρ⁰] + V_xc[ρ⁰]

3. 解 KS 方程:
   [-ℏ²/(2m)∇² + V_eff] φᵢ = εᵢ φᵢ
   得到一组轨道 {φᵢ} 和能量 {εᵢ}

4. 从轨道构造新密度:
   ρ¹(r) = Σᵢ |φᵢ(r)|²

5. 检查收敛:
   |ρ¹ - ρ⁰| < 阈值?
   
   是 → 收敛，输出结果
   否 → 混合 ρ_new = α·ρ¹ + (1-α)·ρ⁰，回到第 2 步
```

**为什么要混合？** 如果直接用新密度 $\rho^1$ 替换旧密度 $\rho^0$，迭代往往会震荡不收敛。混合（damping）可以稳定迭代。实际中使用更高级的混合算法如 DIIS（Direct Inversion in the Iterative Subspace）。

### 3.7 KS 方程的精确性讨论

**一个关键事实**：如果我们知道精确的 $E_{\text{xc}}[\rho]$，KS 方案给出的**密度和总能量是精确的**。

也就是说，KS 方案本身**不引入近似**——所有的近似都集中在 $E_{\text{xc}}[\rho]$ 这一项。DFT 的精度完全取决于交换-关联泛函的质量。

**关于 KS 轨道 $\phi_i$ 和轨道能量 $\varepsilon_i$**：

- 严格来说，KS 轨道不是物理电子的波函数——它们是辅助系统的产物
- 但在实践中，KS 轨道的形状和化学家心目中的"分子轨道"非常相似
- $\varepsilon_{\text{HOMO}}$ 与实验电离能之间有精确关系（Janak 定理 / Koopmans 类比）
- $\varepsilon_{\text{LUMO}}$ 与电子亲和力的关系则不那么直接
- HOMO-LUMO gap 通常**低估**真实的光学带隙（这是 DFT 带隙问题的根源）

---

## 4. 交换-关联泛函：DFT 的灵魂与软肋

### 4.1 什么是"交换"和"关联"？

**交换（Exchange）**：
- 来自 Pauli 不相容原理：两个同自旋电子不能占据相同的量子态
- 效果：同自旋电子之间有一个"交换空穴"（exchange hole）——它们自动远离彼此
- 降低电子-电子排斥能（交换能为负值）

**关联（Correlation）**：
- 来自电子之间的瞬时库仑排斥
- Hartree 近似假设电子独立运动（平均场），但实际上电子会"协调"自己的运动来避开彼此
- 这个"额外的避让"就是关联效应

**类比**：想象一个拥挤的广场上的人群

- Hartree：每个人只看到平均人口密度，不管周围有没有人（会踩到脚）
- + 交换：认识的人自动保持距离（同自旋的 Pauli 排斥）
- + 关联：不认识的人也会下意识地避开（瞬时库仑排斥的动态回避）

### 4.2 Jacob's Ladder：泛函的精度阶梯

John Perdew 提出了著名的"Jacob's Ladder"（雅各布天梯）比喻——泛函近似按精度从低到高排列：

#### 第一阶梯：LDA（Local Density Approximation，局域密度近似）

**思想**：假设每一点的 $E_{\text{xc}}$ 只取决于**该点**的密度值，等于**同密度均匀电子气**的交换关联能。

$$E_{\text{xc}}^{\text{LDA}}[\rho] = \int \rho(\vec{r})\,\varepsilon_{\text{xc}}^{\text{homog}}(\rho(\vec{r}))\,d^3r$$

其中 $\varepsilon_{\text{xc}}^{\text{homog}}(\rho)$ 是已知的（可以从均匀电子气的量子蒙特卡洛模拟精确得到）。

- **交换部分**有解析公式：$\varepsilon_x(\rho) = -\frac{3}{4}\left(\frac{3}{\pi}\right)^{1/3}\rho^{1/3}$
- **关联部分**由 Ceperley-Alder (1980) 的 QMC 数据参数化（VWN, PW92 等）

**优点**：简单，无经验参数
**缺点**：键长偏短 ~1-2%，键能严重高估 ~20%，带隙严重低估

#### 第二阶梯：GGA（Generalized Gradient Approximation，广义梯度近似）

**思想**：不仅考虑每点的密度 $\rho$，还考虑密度的**梯度** $\nabla\rho$（密度变化快不快）。

$$E_{\text{xc}}^{\text{GGA}}[\rho] = \int \rho(\vec{r})\,\varepsilon_{\text{xc}}(\rho, |\nabla\rho|)\,d^3r$$

常见 GGA 泛函：
- **PBE**（Perdew-Burke-Ernzerhof, 1996）：无经验参数，固体物理首选
- **BLYP**（Becke88 交换 + Lee-Yang-Parr 关联）：化学常用
- **BP86**：早期常用

**改进**：键能误差降到 ~5%，键长准确到 ~0.01 Å

#### 第三阶梯：Meta-GGA

在 GGA 基础上加入动能密度 $\tau(\vec{r}) = \frac{1}{2}\sum_i |\nabla\phi_i|^2$：

$$E_{\text{xc}}^{\text{meta-GGA}} = \int f(\rho, \nabla\rho, \tau)\,d^3r$$

代表：TPSS, SCAN, r²SCAN

#### 第四阶梯：杂化泛函（Hybrid Functionals）——有机电子学的主力

**核心思想**：混入一部分 Hartree-Fock **精确交换**能。

为什么？因为 GGA 的交换能有"自相互作用误差"（self-interaction error）——一个电子排斥自己。混入 HF 交换可以部分修正这个问题。

**B3LYP（最著名的杂化泛函）**：

$$E_{\text{xc}}^{\text{B3LYP}} = (1-a)E_x^{\text{LDA}} + a\,E_x^{\text{HF}} + b\,\Delta E_x^{\text{B88}} + (1-c)E_c^{\text{VWN}} + c\,E_c^{\text{LYP}}$$

参数：$a = 0.20$, $b = 0.72$, $c = 0.81$（拟合热化学数据得到）

- 20% HF 交换 + 80% DFT 交换
- LYP 关联 + 部分 VWN 关联

**PBE0**：25% HF 交换 + 75% PBE 交换 + PBE 关联（无经验参数，比例 1/4 有理论依据）

**杂化泛函是有机分子计算的标准选择**：
- 键长准确到 0.005 Å
- 振动频率准确到 ~30 cm⁻¹（乘以标度因子后更好）
- 热化学误差 ~3 kcal/mol

#### 第五阶梯：长程修正杂化泛函

**问题**：B3LYP 用固定比例的 HF 交换。但物理上，近距离和远距离的交换行为应该不同：

- 近距离：DFT 交换近似较好
- 远距离：HF 交换才能给出正确的 $-1/r$ 渐近行为

**解决方案**：用误差函数把交换分为短程和长程两部分：

$$\frac{1}{r_{12}} = \frac{\text{erfc}(\omega r_{12})}{r_{12}} + \frac{\text{erf}(\omega r_{12})}{r_{12}}$$

短程用 DFT 交换，长程用 HF 交换。参数 $\omega$ 控制切换距离。

代表泛函：
- **ωB97X-D**：长程修正 + 色散修正
- **CAM-B3LYP**：Coulomb-attenuating method

**何时必须用长程修正？**
- 电荷转移激发态（CT states）
- Rydberg 态
- 大分子的极化率
- 有机太阳能电池中的 D-A 界面

### 4.3 色散修正（van der Waals 力）

标准 DFT 泛函**完全缺失**分子间的 van der Waals（色散）作用力。对于有机半导体（分子间堆叠靠 π-π 相互作用），这是致命缺陷。

**DFT-D 方法**（Grimme）：

在 DFT 能量后面加一个半经验的 $-C_6/R^6$ 修正项：

$$E_{\text{DFT-D}} = E_{\text{DFT}} - \sum_{A<B} f_{\text{damp}}(R_{AB})\frac{C_6^{AB}}{R_{AB}^6}$$

- D3(BJ)：最常用的色散修正版本
- 对分子间距离和晶体结构至关重要

### 4.4 如何选择泛函？（实用指南）

| 你要算什么 | 推荐泛函 | 原因 |
|-----------|---------|------|
| 分子几何优化 | B3LYP-D3(BJ)/6-31G(d) | 性价比最高 |
| 准确的能量 | ωB97X-D/def2-TZVP | 长程修正 + 色散 |
| HOMO/LUMO | PBE0 或 ωB97X-D | 杂化泛函减小带隙误差 |
| 激发态/光谱 (TD-DFT) | CAM-B3LYP 或 ωB97X-D | CT 态需要长程修正 |
| 固体/周期性体系 | PBE+D3 或 HSE06 | PBE 经济，HSE06 带隙好 |
| 分子间相互作用 | ωB97X-D 或 B3LYP-D3 | **必须**有色散修正 |
| 重组能 $\lambda$ | B3LYP/6-31G(d,p) | 文献标准，便于比较 |

---

## 5. 基组：用什么函数展开 KS 轨道

### 5.1 为什么需要基组？

KS 方程的解 $\phi_i(\vec{r})$ 是连续函数。计算机不能处理连续函数，需要把它展开为有限个已知函数的线性组合：

$$\phi_i(\vec{r}) = \sum_{\mu=1}^{K} c_{\mu i}\,\chi_\mu(\vec{r})$$

$\{\chi_\mu\}$ 就是**基组**（basis set）——一组预先选定的函数。$K$ 是基组大小。

把 KS 方程代入，得到矩阵方程（Roothaan 方程）：

$$\mathbf{F}\mathbf{C} = \mathbf{S}\mathbf{C}\boldsymbol{\varepsilon}$$

- $\mathbf{F}$：$K\times K$ Fock/KS 矩阵
- $\mathbf{S}$：$K\times K$ 重叠矩阵 $S_{\mu\nu} = \int \chi_\mu^*\chi_\nu\,d^3r$
- $\mathbf{C}$：$K\times K$ 系数矩阵
- $\boldsymbol{\varepsilon}$：轨道能量

解这个方程 = 对角化一个 $K\times K$ 的矩阵。$K=100$ 时瞬间完成，$K=1000$ 时几分钟。

### 5.2 高斯型轨道（GTO）

量子化学几乎全部使用**高斯型函数**作为基函数：

$$\chi(\vec{r}) = N \cdot x^l y^m z^n \cdot e^{-\alpha|\vec{r}-\vec{R}_A|^2}$$

其中 $(l,m,n)$ 决定角动量类型：
- $l=m=n=0$：s 型
- $(1,0,0), (0,1,0), (0,0,1)$：p 型（三个）
- $(2,0,0), (0,2,0), (0,0,2), (1,1,0), (1,0,1), (0,1,1)$：d 型（六个）

**为什么用高斯而不是 Slater 型函数（STO，$e^{-\zeta r}$）？**

物理上，真实的原子轨道更像 STO。但两个 GTO 的乘积还是一个 GTO（高斯函数乘法的性质），这使得双电子积分可以解析计算——效率提高几个数量级。

一个 STO 通常用 3–6 个 GTO 的线性组合来拟合（"收缩"）。

### 5.3 常用基组的命名规则

#### Pople 基组（6-31G 家族）

`6-31G(d,p)` 的含义：

| 部分 | 含义 |
|------|------|
| `6` | 内层轨道（core）用 6 个 GTO 拟合 |
| `31` | 价层轨道分两组：3 个 GTO（紧缩部分）+ 1 个 GTO（弥散部分） |
| `(d)` | 重原子（C, N, O...）加 d 型极化函数 |
| `(d,p)` | 重原子加 d，氢原子加 p 型极化函数 |
| `+` | 加弥散函数（用于阴离子、弱相互作用）|

大小递增：STO-3G < 6-31G < 6-31G(d) < 6-31G(d,p) < 6-311+G(2d,p)

#### Dunning 基组（cc-pVXZ 家族）

"correlation-consistent, polarized Valence, X-Zeta"

$$\text{cc-pVDZ} < \text{cc-pVTZ} < \text{cc-pVQZ} < \text{cc-pV5Z} \to \text{CBS}$$

D=Double, T=Triple, Q=Quadruple. 系统性收敛到完备基组极限（CBS）。

#### Ahlrichs/Karlsruhe 基组（def2 家族）

- def2-SVP（split-valence + polarization）≈ 6-31G(d)
- def2-TZVP ≈ 6-311G(2d,p)
- def2-QZVP ≈ cc-pVQZ

### 5.4 基组选择的实用原则

| 目的 | 推荐基组 | 每个重原子的基函数数 |
|------|---------|-------------------|
| 快速几何预优化 | 6-31G(d) | ~15 |
| 标准计算 | 6-31G(d,p) 或 def2-SVP | ~19 |
| 准确能量 | def2-TZVP 或 cc-pVTZ | ~35 |
| 基准计算 | def2-QZVP 或 cc-pVQZ | ~55 |
| 阴离子/弱相互作用 | 6-31+G(d) 或 aug-cc-pVDZ | 加弥散函数 |

> **经验法则**：几何优化可以用较小基组（6-31G(d)），能量单点计算用大基组（def2-TZVP）。这叫 "opt small / energy large" 策略。

### 5.5 基组重叠误差（BSSE）

当计算分子间相互作用时，两个分子靠近后可以"借用"对方的基函数，人为降低能量。这叫基组重叠误差（Basis Set Superposition Error）。

修正方法：**Counterpoise 校正**（Boys-Bernardi 方法）——每个分子单独计算时也包含对方的基函数（作为"ghost atoms"）。

---

## 6. 实际计算流程详解

### 6.1 完整的计算流水线

以"计算一个有机分子的重组能 $\lambda$"为例，这是有机电子学中最常见的 DFT 计算之一：

```
第一阶段：几何优化（中性分子）
  输入：初始坐标（可以从晶体结构或分子编辑器生成）
  方法：B3LYP/6-31G(d,p)
  输出：优化几何 Q₀，能量 E(Q₀)

第二阶段：几何优化（阳离子）
  输入：中性分子的优化构型
  方法：UB3LYP/6-31G(d,p)，电荷=+1，多重度=2
  输出：阳离子优化几何 Q⁺，能量 E⁺(Q⁺)

第三阶段：单点能量计算
  (a) 中性分子在阳离子几何下：E(Q⁺)
  (b) 阳离子在中性分子几何下：E⁺(Q₀)

第四阶段：计算重组能
  λ₁ = E(Q⁺) - E(Q₀)       # 中性→阳离子几何的弛豫能
  λ₂ = E⁺(Q₀) - E⁺(Q⁺)    # 阳离子→中性几何的弛豫能
  λ = λ₁ + λ₂               # 总内重组能
```

### 6.2 SCF 过程中每一步的计算量

以蒽 (C₁₄H₁₀) + B3LYP/6-31G(d) 为例（$K \approx 250$ 个基函数）：

#### Step 1：计算单电子积分（很快）

- 动能积分：$T_{\mu\nu} = \langle\chi_\mu|-\frac{1}{2}\nabla^2|\chi_\nu\rangle$
- 核吸引积分：$V_{\mu\nu}^{\text{nuc}} = \langle\chi_\mu|\sum_A \frac{-Z_A}{|\vec{r}-\vec{R}_A|}|\chi_\nu\rangle$
- 重叠积分：$S_{\mu\nu} = \langle\chi_\mu|\chi_\nu\rangle$

数量：$\sim K^2/2 \approx 31000$ 个。每个积分有解析公式，瞬间完成。

#### Step 2：计算双电子积分（最耗时）

$$(\mu\nu|\lambda\sigma) = \iint \chi_\mu(\vec{r}_1)\chi_\nu(\vec{r}_1) \frac{1}{|\vec{r}_1 - \vec{r}_2|} \chi_\lambda(\vec{r}_2)\chi_\sigma(\vec{r}_2)\,d^3r_1\,d^3r_2$$

数量：$\sim K^4/8 \approx 5 \times 10^8$ 个。这是 DFT 计算中最耗时的部分。

实际中通过筛选（screening）和密度拟合（RI/DF）技术大大减少需要计算的积分数。

#### Step 3：构造 Fock/KS 矩阵

$$F_{\mu\nu} = h_{\mu\nu} + \sum_{\lambda\sigma} P_{\lambda\sigma}\left[(\mu\nu|\lambda\sigma) - \frac{\alpha}{2}(\mu\lambda|\nu\sigma)\right] + V_{\mu\nu}^{\text{xc}}$$

其中 $\alpha = 0.20$（B3LYP 的 HF 交换比例）。

$V_{\mu\nu}^{\text{xc}}$ 通过数值积分计算：在空间中布 ~5000–50000 个格点，每个格点上算密度和梯度，代入泛函公式。

#### Step 4：对角化

解 $\mathbf{F}\mathbf{C} = \mathbf{S}\mathbf{C}\boldsymbol{\varepsilon}$。对 $250 \times 250$ 矩阵做广义本征值分解，$< 1$ 毫秒。

#### Step 5：构造新密度矩阵

$$P_{\mu\nu} = 2\sum_{i=1}^{N/2} c_{\mu i}\,c_{\nu i}$$

矩阵乘法，瞬间完成。

#### 重复 Steps 2–5 直到收敛（~10-20 轮）

### 6.3 几何优化的流程

SCF 收敛后得到一个固定几何下的能量 $E(\{\vec{R}_A\})$。要找平衡几何，需要：

1. **计算梯度**（每个原子上的力）：

$$\vec{F}_A = -\frac{\partial E}{\partial \vec{R}_A}$$

这需要**解析梯度**技术（不是有限差分），通过 Hellmann-Feynman 定理加修正项计算。

2. **判断收敛**：
   - 最大力 < 0.00045 au
   - RMS 力 < 0.00030 au
   - 最大位移 < 0.0018 au
   - RMS 位移 < 0.0012 au

3. **更新几何**（准牛顿法）：
   - 需要 Hessian 矩阵（能量的二阶导数）
   - 初始 Hessian 用经验力场估计
   - 每步用 BFGS 公式更新 Hessian

4. **回到 SCF**：新几何 → 新外势 → 新 SCF

典型的有机分子需要 10–30 步几何迭代。

### 6.4 频率计算

在优化几何上计算 Hessian（力常数矩阵）：

$$H_{Ai,Bj} = \frac{\partial^2 E}{\partial R_{Ai}\partial R_{Bj}}$$

其中 $R_{Ai}$ 是原子 $A$ 的第 $i$ 个坐标分量。

对于 $N_{\text{atom}}$ 个原子的分子，Hessian 是 $3N_{\text{atom}} \times 3N_{\text{atom}}$ 矩阵。

质量加权后对角化：

$$\tilde{H}_{Ai,Bj} = \frac{H_{Ai,Bj}}{\sqrt{m_A m_B}}$$

本征值 $\lambda_k = \omega_k^2$，本征向量 = 简正模式方向。

- 6 个零频率模式（3 平动 + 3 转动）
- $3N_{\text{atom}} - 6$ 个振动模式

**如果出现虚频**（$\omega^2 < 0$）：说明该点不是势能面极小值，而是鞍点——需要沿虚频方向调整几何，重新优化。

---

## 7. DFT 的已知局限性

### 7.1 带隙问题

KS-DFT 系统性地**低估** HOMO-LUMO 能隙。原因：

1. **KS 能隙 ≠ 基本能隙**：基本能隙 = IP - EA，而 KS 能隙 = $\varepsilon_{\text{LUMO}} - \varepsilon_{\text{HOMO}}$ 少了一个"导数不连续性"（derivative discontinuity）
2. **自相互作用误差**：标准泛函中，一个电子会排斥自己（Coulomb 自作用没有完全消除），导致被占轨道能量偏高、空轨道能量偏低

**实际影响**：
- LDA/GGA：带隙低估 30–50%
- B3LYP：低估 10–20%
- 长程修正杂化泛函（ωB97X-D）：接近实验值

### 7.2 自相互作用误差（SIE）

在精确理论中，一个电子不应该和自己相互作用。但在 DFT 中：

$$E_H[\rho] = \frac{1}{2}\iint \frac{\rho(\vec{r})\rho(\vec{r}')}{|\vec{r}-\vec{r}'|}d^3r\,d^3r'$$

当 $\rho$ 只包含一个电子时，$E_H$ 不为零——电子在排斥自己！

精确的交换能应该**完全抵消**这个自相互作用，但近似的 $E_{\text{xc}}$ 做不到。

**后果**：
- 电子过于离域（delocalized）
- 电荷转移态能量偏低
- 反应能垒偏低
- 磁性耦合描述不准

### 7.3 色散力缺失

LDA 和 GGA 完全不能描述 van der Waals 作用力（$-C_6/R^6$），因为这是一个**非局域关联效应**——它取决于两个远处区域的瞬时极化的耦合。

对有机半导体的影响：
- 分子间距离错误（通常偏大）
- π-π 堆叠能量被严重低估或完全缺失
- 晶体结构预测不准

**必须**加色散修正（DFT-D3, DFT-D4, 或 TS 方法）。

### 7.4 强关联问题

当电子关联非常强时（比如过渡金属氧化物、开壳层双自由基），单行列式的 KS 方案可能失败。

这时需要：
- 多参考方法（CASSCF, CASPT2）
- 或 broken-symmetry DFT（近似处理）

对于大多数有机半导体的闭壳层基态，这通常不是问题。但对于某些非富勒烯受体中的双自由基特性，需要注意。

### 7.5 激发态

标准 DFT 是**基态理论**。激发态需要 **TD-DFT**（Time-Dependent DFT）：

- 基于 Runge-Gross 定理（HK 定理的含时版本）
- 线性响应框架：$\omega_n = E_n - E_0$ 从响应方程的极点得到
- 精度取决于泛函选择：
  - 局域激发：B3LYP 通常够用
  - 电荷转移态：**必须**用长程修正泛函

---

## 8. DFT 在有机电子学中的核心应用

### 8.1 分子轨道与能级排列

```
真空能级 ────────────────── 0 eV
         │
         │  IP ≈ -ε_HOMO (Koopmans/Janak)
         │
HOMO ────┤──── 电离能 → 与阳极功函匹配 → 空穴注入
         │
         │  E_gap ≈ ε_LUMO - ε_HOMO (低估)
         │
LUMO ────┤──── 电子亲和力 → 与阴极功函匹配 → 电子注入
         │
```

DFT 给出的 HOMO/LUMO 能量可以估计：
- 电离能 (IP) ≈ $-\varepsilon_{\text{HOMO}}$
- 电子亲和力 (EA) ≈ $-\varepsilon_{\text{LUMO}}$（精度较差）
- 更准确的做法：$\text{IP} = E(N-1) - E(N)$，$\text{EA} = E(N) - E(N+1)$（ΔSCF 方法）

### 8.2 重组能计算

内重组能 $\lambda_i$ 通过"四点法"计算：

$$\lambda_i = \lambda_1 + \lambda_2$$

$$\lambda_1 = E_{\text{neutral}}(Q^+) - E_{\text{neutral}}(Q^0)$$

$$\lambda_2 = E_{\text{cation}}(Q^0) - E_{\text{cation}}(Q^+)$$

这和第四讲中 Huang-Rhys 因子的关系是：$\lambda_i = S\hbar\omega$（对单模近似）。

### 8.3 转移积分计算

分子间的电荷转移积分 $t_{ij}$ 可以通过：
- 能量劈裂法：$t = (E_{\text{LUMO+1}} - E_{\text{LUMO}})/2$（对称二聚体）
- 投影法（更一般）

### 8.4 光谱计算（TD-DFT）

| 性质 | 方法 | 输出 |
|------|------|------|
| 紫外-可见吸收 | TD-DFT | 激发能 + 振子强度 |
| 荧光发射 | TD-DFT (excited-state optimization) | S₁ 几何的垂直发射能 |
| 振动光谱 | 频率计算 | IR/Raman 峰位置和强度 |
| 振动精细结构 | Franck-Condon 分析 | 0-0, 0-1, 0-2 峰的相对强度 |

### 8.5 一个完整的例子：蒽的光物理性质

```
# Gaussian 输入（几何优化）
%nproc=8
%mem=4GB
#p B3LYP/6-31G(d,p) opt freq

anthracene optimization

0 1
[蒽的坐标]

# 得到：优化几何、振动频率、IR 强度

# TD-DFT 激发态计算
#p TD=(NStates=10) CAM-B3LYP/6-311+G(2d,p)

# 得到：S₁ 激发能（实验 ~3.3 eV，计算 ~3.4 eV）、振子强度

# 重组能计算
# 中性优化 → 阳离子优化 → 交叉单点
# 得到：λ ≈ 0.13 eV（实验 ~0.12 eV）
```

---

## 9. DFT 与前面课程内容的联系总结

| 前面学过的 QM 概念 | 在 DFT 中如何出现 |
|-------------------|------------------|
| 薛定谔方程 | KS 方程的形式完全相同 |
| 粒子在势阱中 | KS 方程 = 粒子在有效势 $V_{\text{eff}}$ 中 |
| 变分原理 | HK 第二定理 + KS 方程的推导 |
| 量子谐振子 | 频率计算 = 在势能面极值点的谐振子近似 |
| Franck-Condon | TD-DFT + FC 分析 → 振动光谱模拟 |
| Huang-Rhys 因子 | $S = \lambda_i / \hbar\omega$，DFT 计算 $\lambda_i$ |
| Born-Oppenheimer 近似 | DFT 只解电子部分，核通过势能面处理 |
| 不确定性原理 | 动能泛函 $T_s$ 的物理根源 |

---

## 10. 常见陷阱与注意事项

### 10.1 计算设置陷阱

| 错误 | 后果 | 正确做法 |
|------|------|---------|
| 几何优化后不做频率验证 | 可能优化到鞍点 | **总是**做 `freq` 检查无虚频 |
| 阴离子计算不加弥散函数 | 电子"跑掉"，能量不收敛 | 阴离子用 6-31+G(d) 或 aug- 基组 |
| 开壳层不用 unrestricted | 自旋污染，能量错误 | 阳离子/阴离子用 `U` 前缀 |
| π-π 堆叠不加色散修正 | 排斥势能面，无法描述堆叠 | B3LYP-D3(BJ) 或 ωB97X-D |
| CT 态用 B3LYP | 激发能严重低估 | 用 CAM-B3LYP 或 ωB97X-D |
| 大分子用太大基组 | 计算不完 | 先 opt/6-31G(d)，再 SP/大基组 |

### 10.2 结果验证策略

1. **频率检查**：优化后必须全正频
2. **基组收敛测试**：换更大基组看能量变化是否 < 目标精度
3. **泛函依赖性测试**：用 2-3 个不同泛函比较结果
4. **对比实验**：如果有实验数据，先验证方法可靠性
5. **文献对比**：看同类分子的计算用什么方法、得到什么精度

---

## 11. 课堂互动问题

1. 为什么 DFT 的计算量标度是 $O(K^3)$ 而不是指数增长？关键简化在哪一步？

2. HK 第一定理的证明中用到了变分原理。如果基态是简并的（两个波函数对应同一个能量），证明还成立吗？为什么？

3. B3LYP 中的 "20% HF 交换"是怎么来的？这个数字有没有理论依据，还是纯粹经验拟合？

4. 一个学生用 B3LYP/6-31G(d) 优化了分子 A 的几何，然后用同一方法计算了分子 B 的单点能，声称两者的能量可以直接比较。这样做对吗？在什么条件下能量才可比？

5. 你在计算一个有机太阳能电池的 D-A 界面 CT 态。你用 B3LYP/6-31G(d) 做 TD-DFT，得到 CT 态能量 0.8 eV，但实验值是 1.5 eV。可能的原因是什么？如何修正？

---

## 12. 课后作业建议

### 12.1 概念题

1. 用自己的话解释：为什么 Kohn-Sham 方案把"一个不可解的多体问题"变成了"一组可解的单体问题"？哪一步引入了近似？

2. Thomas-Fermi 模型直接把动能写成 $T_{\text{TF}}[\rho] = C_F\int\rho^{5/3}\,d^3r$。它为什么不能描述化学键？（提示：想想均匀密度假设对于分子的什么性质是致命的。）

3. 解释为什么长程修正泛函对电荷转移态是必需的。画一个电子-空穴对的示意图，说明远程 $-1/r$ 行为为什么重要。

### 12.2 计算练习

（如果有 Gaussian 或其他量子化学程序的使用权限）

1. 取乙烯 (C₂H₄) 分子：
   (a) 用 B3LYP/6-31G(d) 优化几何，记录 C=C 键长
   (b) 做频率计算，找到 C=C 伸缩模式的频率，与实验值 (1623 cm⁻¹) 比较
   (c) 用 HF/6-31G(d) 重复，比较差异

2. 取萘 (C₁₀H₈) 分子：
   (a) 优化中性和阳离子几何
   (b) 用四点法计算内重组能
   (c) 哪个键长变化最大？这和你对 π→π* 跃迁的理解一致吗？

---

## 13. 参考文献

**入门教材：**
- Koch & Holthausen, *A Chemist's Guide to Density Functional Theory*, 2nd ed. (Wiley, 2001) — 化学家视角的 DFT 入门
- Sholl & Steckel, *Density Functional Theory: A Practical Introduction* (Wiley, 2009) — 侧重材料科学应用

**经典论文：**
- Hohenberg & Kohn, Phys. Rev. 136, B864 (1964) — HK 定理原始论文
- Kohn & Sham, Phys. Rev. 140, A1133 (1965) — KS 方程原始论文

**泛函选择指南：**
- Mardirossian & Head-Gordon, Mol. Phys. 115, 2315 (2017) — 泛函系统性评估

**有机电子学 DFT 应用：**
- Brédas et al., Chem. Rev. 104, 4971 (2004) — 有机半导体的量子化学计算
- K&B (Köhler & Bässler), *Electronic Processes in Organic Semiconductors*, §1.3–1.4

---

## 14. 讲义总结：一页纸版本

```
┌─────────────────────────────────────────────────────────────┐
│                     DFT 原理速查                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  多体问题: Ĥ Ψ(r₁...rₙ) = E Ψ  ← 不可解(3N 维)          │
│                     ↓                                       │
│  HK 定理: ρ₀(r) 唯一确定一切 ← 3 维就够                   │
│                     ↓                                       │
│  KS 方案: 引入无相互作用辅助系统                            │
│     [-ℏ²/2m ∇² + V_eff(r)] φᵢ = εᵢ φᵢ                    │
│     V_eff = V_ext + V_H + V_xc                             │
│                     ↓                                       │
│  自洽求解: ρ → V_eff → φᵢ → ρ_new → 收敛?                │
│                     ↓                                       │
│  近似在哪: E_xc[ρ] (LDA < GGA < Hybrid < Range-separated) │
│                                                             │
│  关键输出: εᵢ(轨道能), E₀(总能量), ρ(r)(密度), ∂E/∂R(力) │
│                                                             │
│  有机电子学: HOMO/LUMO, λ=S·ℏω, 转移积分, TD-DFT光谱     │
│                                                             │
│  必记: 色散修正(D3), CT态用长程修正, 频率验证无虚频         │
└─────────────────────────────────────────────────────────────┘
```
